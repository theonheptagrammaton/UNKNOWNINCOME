export default function BacktestLabPage() {
  return (
    <section className="flex flex-col gap-4">
      <span className="text-xs uppercase tracking-[0.3em] text-fog-faint">
        Page 1
      </span>
      <h1 className="text-3xl font-semibold tracking-tight">Backtest Lab</h1>
      <p className="max-w-xl text-fog-muted">
        Scan builder, leaderboard and run detail arrive in Phase 3. This is the
        empty shell.
      </p>
    </section>
  );
}
